function quiverSS(A,X)
% velocity plot

x1 = X.simout.Data(:,1);
x2 = X.simout.Data(:,2);
n = 8;

x = [linspace(min(x1),max(x1),n);linspace(min(x2),max(x2),n)];
%x = [x1';x2'];

x1 = x(1,:);
x2 = x(2,:);

[x1,x2] = meshgrid(x1,x2);
dx1 = A()

quiver(x(1,:),x(2,:),xx(1,:),xx(2,:));

end